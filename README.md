# Neovim Config

###Plugins

    - blink.cmp
        - Install rustup
        - If you need to update, remove Cargo lock file, cd into the plugin's directory, run:
        **rustup update**
        **cargo build --release**
